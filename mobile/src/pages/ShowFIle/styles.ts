import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    fileContainer:{
        flex: 1,
        borderColor: '#333',
        borderWidth: .2,
        marginTop: 70,
        marginBottom: 120,
        width: '100%'
    },
    fileContent:{
        flex: 1
    }
})

export {styles}